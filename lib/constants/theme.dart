import 'package:flutter/material.dart';

ThemeData lightTheme =
    ThemeData(brightness: Brightness.light, primaryColor: Colors.redAccent);
ThemeData darkTheme =
    ThemeData(brightness: Brightness.dark, primaryColor: Colors.redAccent);
